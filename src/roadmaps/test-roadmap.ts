import { roadmap } from 'trellisuml';
import { default as eshop } from '../solutions/eshop_solution';

export default roadmap({
  name: 'Test Roadmap',
  description: 'Test roadmap used to evaluate syntax',
  solutions: [
    eshop,
  ],
  phases: [],
});
